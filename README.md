# Java Array Assignment

## Description

A Java program that takes numbers entered by the user and calculates:

- Sum
- Average
- Largest value
- Smallest value
- Sorted values

The program uses a separate method for each calculation and `printf()` for aligned output.

```markdown
## How to Run

Compile the program:

```bash
javac ArrayFormattedOutput.java
